// Host-side modern C++20 CCSDS Space Packet codec.
// Interface designed in Step 4 — this placeholder lets the build loop close
// from day one so CI is green before any real logic lands.
#pragma once

namespace ccsds {

// Scaffolding sentinel — replaced in Step 4 by the real interface.
inline constexpr int scaffolding_sentinel() { return 0x29B1; }

}  // namespace ccsds
